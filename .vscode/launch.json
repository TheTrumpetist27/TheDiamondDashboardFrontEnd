{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/the-diamond-dashboard-frontend/package.json",
                "localRoot": "${workspaceFolder}/the-diamond-dashboard-frontend"
            }
        }
    ]
}